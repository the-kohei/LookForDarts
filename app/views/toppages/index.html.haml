= render 'shared/header'
.main
  .main__top
    .main__top--content
      %h2 お気に入りのダーツを共有しよう
.contain
  .contain__title
    .contain__title__text
      %h2 LookForDartsとは
      %h4 ダーツってたくさん種類があって迷いますよね？
      %h4 そんな方へのおすすめのダーツの口コミ共有サービスです。
  .main-page
    .product
      .product__new
        %h3 新着口コミ
    .products
      - @posts.each do |post|
        .products__item
          .products__item__image
            =link_to image_tag(post.image.to_s, height: '100%', width: '100%', class: 'iamge10'), "/posts/#{post.id}"
          .products__item__body
            .products__item__body__title 
              %a
                = link_to "/posts/#{post.id}" do
                  = post.title
            .products__item__body__contents
              .products__item__body__contents__left
                .products__item__body__contents__left__menu
                %ul
                  カテゴリー：
                %ul
                  メーカー :
                %ul
                  投稿者 :
              .products__item__body__contents__right
                .products__item__body__contents__right__link
                  %li
                    = link_to post.category.name, category_posts_posts_path(category_id: post.category.id)
                  %li
                    = link_to post.maker.name, maker_posts_posts_path(maker_id: post.maker.id)
                  %li
                    = link_to post.user.name, user_posts_posts_path(user_id: post.user.id)
= render 'shared/shop'
= render 'shared/footer'
